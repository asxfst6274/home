<% if(success){ %>
<div class="bg-success p-3 m-3 rounded flex justify-center">
  <strong><%= message %></strong>
</div>
<% } %> <% if(error){ %>
<div class="bg-danger text-white p-3 m-3 rounded flex justify-center">
  <strong><%= message %></strong>
</div>
<% } %>
